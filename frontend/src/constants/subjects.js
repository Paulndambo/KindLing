/**
 * Soft fallback topics only when a lesson is opened without any of the
 * student's subjects/topics selected. Prefer real student subjects from the API.
 */
export const DEFAULT_LESSON_TOPICS = ["Introduction"];
